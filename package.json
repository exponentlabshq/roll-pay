{
  "name": "roll-pay",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Roll — the digital-dollar card where every tap is a chance to pay nothing.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
